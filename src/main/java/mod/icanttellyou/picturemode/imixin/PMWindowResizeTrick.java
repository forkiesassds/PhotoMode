//? if >=26.1 && <26.2 {
/*package mod.icanttellyou.picturemode.imixin;

/^*
 * A mixin helper to implement a specific trick for Picture Mode
 ^/
public interface PMWindowResizeTrick {
    /^*
     * Marks the window to be forcibly resized.
     ^/
    void pictureMode$forciblyResizeWindow();
}
*///? }