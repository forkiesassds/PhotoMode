//? if >=26.3 {
/*package mod.icanttellyou.picturemode.imixin;

import net.minecraft.resources.Identifier;

/^*
 * A mixin helper for applying post effects
 ^/
public interface PMPostEffectApplier {
    /^*
     * Sets a post effect to use.
     *
     * @param id The ID of the post effect.
     ^/
    void pm$setPostEffect(Identifier id);

    /^*
     * Clears the post effect, if any are set.
     ^/
    void pm$clearPostEffect();
}
*///? }