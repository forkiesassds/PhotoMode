//? if >=26.2 {
/*package mod.icanttellyou.picturemode.imixin;

/^*
 * A mixin helper for skipping blitting and presenting frames to the screen.
 ^/
public interface PMSkippableGpuSurface {
    /^*
     * Tells the surface to be skipped.
     * The frame will not be blitted and presented to the screen.
     ^/
    void pm$skipFrame();
}
*///? }